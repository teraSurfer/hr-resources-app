import Departments from '@/components/dashboard/departments/FindDepartment.vue';

describe('Components -> Departments.vue', () => {
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
    it('Should have data', () => {
        expect(typeof Departments.data).toBe('function')
    })
})