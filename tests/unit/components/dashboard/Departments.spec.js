import Departments from '@/components/dashboard/departments/Departments.vue';

describe('Components -> Departments.vue', () => {
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
    it('Should have items', () => {
        expect(typeof Departments.computed.items).toBe('function')
    })
})